# Copy to .env and set this to your Docker Hub username or organization.
DOCKERHUB_NAMESPACE=your-dockerhub-username

# Optional. Leave empty for normal Docker Hub tags like username/image:tag.
# Set to docker.io if you prefer fully qualified references.
# REGISTRY=docker.io
